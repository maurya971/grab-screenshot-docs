{
  "name": "pixcap",
  "version": "1.0.0",
  "description": "Use ffmpeg to extract an image at a given timestamp for a given video",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "build:routes": "tsoa routes",
    "build:swagger": "tsoa swagger",
    "build:all": "npm run build:routes && npm run build:swagger && npm run build",
    "start": "node dist/main.js",
    "lint": "tslint -c tslint.json 'src/**/*.ts'"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:maurya971/grab-screenshot.git"
  },
  "author": "Ashutosh Maurya",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/fluent-ffmpeg": "^2.1.20",
    "@types/jest": "^27.4.0",
    "@types/node": "^17.0.8",
    "@types/supertest": "^2.0.11",
    "jest": "^27.4.7",
    "supertest": "^6.1.6",
    "ts-jest": "^27.1.2",
    "tsoa": "^3.14.1",
    "typescript": "^4.5.4"
  },
  "dependencies": {
    "@types/swagger-ui-express": "^4.1.3",
    "dotenv": "^10.0.0",
    "express": "^4.17.2",
    "fluent-ffmpeg": "^2.1.2",
    "swagger-ui-express": "^4.3.0"
  }
}
